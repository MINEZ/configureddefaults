package fuzs.configureddefaults.neoforge.client;

import fuzs.configureddefaults.ConfiguredDefaults;
import net.neoforged.api.distmarker.Dist;
import net.neoforged.fml.common.Mod;

@Mod(value = ConfiguredDefaults.MOD_ID, dist = Dist.CLIENT)
public class ConfiguredDefaultsNeoForgeClient {

    public ConfiguredDefaultsNeoForgeClient() {
        // NO-OP
    }
}
